# AI 陪伴助手开发任务清单

**基于需求文档 V1.0 拆解**  
**用途**：作为与 AI 协作开发时的任务输入，以及进度跟踪依据

---

## 第一阶段：核心对话闭环

**阶段目标**：语音对话基本可用，10 分钟连续对话不崩，有基础人格和记忆

### 任务 1.1：项目基础搭建

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-01 |
| **任务名称** | 初始化 Python 项目结构 |
| **任务描述** | 创建模块化项目目录，配置虚拟环境、依赖管理、日志系统、配置文件加载 |
| **交付物** | 可运行的空项目框架，含 README、requirements.txt、config.yaml |
| **验收标准** | 项目能正常启动，日志正常输出，配置可加载 |
| **依赖** | 无 |
| **难度** | ⭐ 简单 |

**目录结构建议：**
```
ai-companion/
├── main.py                 # 入口
├── config.yaml             # 配置文件
├── requirements.txt
├── core/                   # 核心服务
│   ├── conversation.py     # 对话管理
│   ├── personality.py      # 人格引擎
│   ├── memory/             # 记忆系统
│   ├── emotion/            # 情绪系统
│   └── intimacy.py         # 亲密度系统
├── speech/                 # 语音模块
│   ├── asr.py
│   └── tts.py
├── llm/                    # 大模型适配器
│   ├── base.py
│   └── deepseek_api.py
├── ui/                     # 桌面界面
├── tools/                  # 工具模块
├── data/                   # 本地数据
└── logs/                   # 日志
```

---

### 任务 1.2：LLM 适配器层

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-02 |
| **任务名称** | 实现可插拔 LLM 适配器 |
| **任务描述** | 定义 LLM 抽象接口，实现 DeepSeek API 适配器，支持对话生成 |
| **交付物** | `llm/base.py` 抽象类 + `llm/deepseek_api.py` 具体实现 |
| **验收标准** | 传入消息列表，返回生成的回复文本；切换模型无需改动其他代码 |
| **依赖** | T1-01 |
| **难度** | ⭐⭐ 简单偏中 |

**关键代码思路：**
```python
class LLMAdapter(ABC):
    @abstractmethod
    def chat(self, messages: list, system_prompt: str) -> str:
        pass

class DeepSeekAdapter(LLMAdapter):
    def __init__(self, api_key, model="deepseek-chat"):
        ...
    def chat(self, messages, system_prompt):
        # 调用 API，返回回复
        ...
```

---

### 任务 1.3：基础人格提示词系统

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-03 |
| **任务名称** | 编写基础人格 System Prompt |
| **任务描述** | 设计初始人格提示词，包含：温柔体贴的年轻女性、理性冷静的思考方式、正确价值观、不说教式引导 |
| **交付物** | `core/personality.py` + 初始人格 prompt 模板 |
| **验收标准** | LLM 回复风格符合人格设定，无机械感，无说教感 |
| **依赖** | T1-02 |
| **难度** | ⭐⭐⭐ 中等（需要反复调优 prompt） |

**人格 Prompt 要点：**
- 角色设定：年轻女性陪伴者，温柔但有原则
- 说话风格：自然口语，不书面化，偶尔亲昵
- 价值引导：通过提问、分享感受等方式潜移默化，绝不直接批评或说教
- 情绪表达：有自己的情绪，会表现在语言中
- 边界：不盲从用户，有自己的立场

---

### 任务 1.4：语音识别接入（ASR）

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-04 |
| **任务名称** | 接入语音识别模块 |
| **任务描述** | 实现麦克风录音 → 语音转文字功能。前期用云端 API（如讯飞/百度），架构上预留本地替换 |
| **交付物** | `speech/asr.py`，支持实时/准实时转写 |
| **验收标准** | 说话后 1 秒内能输出文字，中文识别准确率 ≥90% |
| **依赖** | T1-01 |
| **难度** | ⭐⭐⭐ 中等 |

**注意：**
- 音频采样率 16kHz，16bit，单声道
- 留出 `BaseASR` 抽象类，后续切换 FunASR 本地方案
- 麦克风用 PyAudio 或 sounddevice 库

---

### 任务 1.5：语音合成接入（TTS）

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-05 |
| **任务名称** | 接入语音合成模块 |
| **任务描述** | 实现文字 → 语音播放。前期用云端 API（推荐火山引擎/讯飞，有情感音色） |
| **交付物** | `speech/tts.py`，支持将 LLM 回复转为语音并播放 |
| **验收标准** | 文字转语音延迟 ≤1 秒，音色为温柔女声 |
| **依赖** | T1-01 |
| **难度** | ⭐⭐ 简单偏中 |

**注意：**
- 选支持流式合成的 API，减少等待
- 预留 `BaseTTS` 抽象类，后期切 GPT-SoVITS / CosyVoice 2
- 语音文件缓存，避免重复合成

---

### 任务 1.6：基础对话管理器

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-06 |
| **任务名称** | 实现对话循环管理 |
| **任务描述** | 串联 ASR → LLM → TTS 的完整对话流程，管理对话状态 |
| **交付物** | `core/conversation.py`，单轮对话完整跑通 |
| **验收标准** | 用户说话 → AI 语音回复，整个流程 2 秒内完成，连续 10 轮不崩 |
| **依赖** | T1-03, T1-04, T1-05 |
| **难度** | ⭐⭐⭐ 中等 |

**状态机：**
```
待机 → 录音中 → 识别中 → 思考中 → 播报中 → 待机
```

---

### 任务 1.7：对话历史存储

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-07 |
| **任务名称** | 实现对话历史持久化 |
| **任务描述** | 用 SQLite 存储所有对话记录，支持按时间查询 |
| **交付物** | `core/memory/db.py`，对话表设计 + 读写接口 |
| **验收标准** | 重启程序后能读取历史对话 |
| **依赖** | T1-01 |
| **难度** | ⭐⭐ 简单偏中 |

**表结构：**
```sql
CREATE TABLE conversations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp DATETIME,
    role TEXT,  -- 'user' or 'assistant'
    content TEXT,
    emotion TEXT,  -- 预留字段
    intimacy_score REAL  -- 预留字段
);
```

---

### 任务 1.8：基础桌面窗口

| 项 | 内容 |
|----|------|
| **任务 ID** | T1-08 |
| **任务名称** | 创建桌面应用窗口 |
| **任务描述** | 用 Electron 或 PyQt 创建基础窗口，显示对话文字 + 形象占位 |
| **交付物** | 可运行的桌面窗口，含对话记录显示区 |
| **验收标准** | 窗口正常显示，对话文字实时滚动 |
| **依赖** | T1-06 |
| **难度** | ⭐⭐ 简单偏中 |

**建议：**  
选择 Electron + Vue，因为后续 Live2D Web SDK 集成更方便。Python 核心通过 WebSocket 与前端通信。

---

### 第一阶段完成标志

✅ 能说 → 能听 → 能回复 → 有声音 → 有界面  
✅ 连续对话 10 分钟不崩溃  
✅ 有基础人格，说话像“她”而不是“机器人”

---

## 第二阶段：情感与记忆深度

**阶段目标**：记忆、情绪、亲密度系统完整，体验接近真人陪伴

### 任务 2.1：用户情绪识别模块

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-01 |
| **任务名称** | 实现用户情绪识别 |
| **任务描述** | 从文本内容分析用户情绪，支持 ≥20 种情绪类别、强度分级、混合情绪 |
| **交付物** | `core/emotion/user_emotion.py` |
| **验收标准** | 输入一段话，能输出情绪类别 + 强度 + 混合情绪标签 |
| **依赖** | T1-06 |
| **难度** | ⭐⭐⭐⭐ 较难 |

**实现方案：**
1. 用 LLM 做情绪分析：输入用户话语 + 上下文，输出 JSON 格式情绪标签
2. 情绪分类体系（示例）：
   - 基础：平静、开心、难过、生气、焦虑、疲惫、孤独、委屈、期待、害羞
   - 复合：既焦虑又期待、表面平静实际难过
   - 强度：1~5 分
3. 后期加入音频声学特征（音高、语速、能量）辅助判断

**输出格式：**
```json
{
  "primary_emotion": "焦虑",
  "intensity": 3,
  "secondary_emotions": ["疲惫"],
  "mixed": "既焦虑又疲惫",
  "confidence": 0.82
}
```

---

### 任务 2.2：AI 情绪系统

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-02 |
| **任务名称** | 实现 AI 独立情绪系统 |
| **任务描述** | 建立多维情绪状态模型，受用户情绪、对话内容、时间/日程、沉默时长影响，随时间自然衰减 |
| **交付物** | `core/emotion/ai_emotion.py` |
| **验收标准** | AI 情绪状态能随交互自然变化，并能被后续模块读取 |
| **依赖** | T2-01 |
| **难度** | ⭐⭐⭐⭐ 较难 |

**情绪模型设计：**
```python
class AIEmotionState:
    # 多维情绪向量（0~1 连续值）
    pleasure: float      # 愉悦度
    arousal: float       # 唤醒度（激动/平静）
    dominance: float     # 支配感
    affection: float     # 对用户的亲近感
    loneliness: float    # 孤独感
    concern: float       # 对用户的担忧
    playfulness: float   # 调皮/趣味性
    
    # 更新规则
    def update(self, user_emotion, dialogue_content, time_context, silence_duration):
        # 用户开心 → pleasure 上升
        # 用户难过 → concern 上升，pleasure 小幅下降
        # 长时间沉默 → loneliness 上升
        # 用户说伤害性话语 → pleasure 下降，affection 下降
        ...
```

**关键规则：**
- 情绪自然衰减：开心不会一直持续
- 情绪惯性：不会瞬间剧烈变化
- 受亲密度调节：亲密度高时，用户情绪对 AI 影响更大

---

### 任务 2.3：亲密度系统

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-03 |
| **任务名称** | 实现五级亲密度系统 |
| **任务描述** | 亲密度分数 0~100，划分五级，动态增减，影响称呼和语言风格 |
| **交付物** | `core/intimacy.py` |
| **验收标准** | 亲密度随互动合理变化，称呼随等级自动切换 |
| **依赖** | T2-02 |
| **难度** | ⭐⭐⭐ 中等 |

**等级与称呼映射：**
| 分数 | 等级 | 称呼示例 |
|------|------|---------|
| 0-20 | 陌生 | “你好” / 用户名字 |
| 21-40 | 熟悉 | 名字 / “你” |
| 41-60 | 亲近 | 昵称 / 小名 |
| 61-80 | 亲密 | 专属昵称 / 亲昵称呼 |
| 81-100 | 挚友/家人 | 最亲昵称呼，自然流露 |

**增减规则（示例）：**

| 事件 | 变化 |
|------|------|
| 一次深度交流（≥5 轮有意义的对话） | +2 |
| 记住并主动提及用户的重要事情 | +3 |
| 安慰用户后用户情绪好转 | +2 |
| 用户说伤害性话语 | -5~-15 |
| 连续 3 天未互动 | -2 |
| 用户真诚道歉 | +3（恢复部分） |
| 日常闲聊 | +0.5 |

**特殊规则：**
- 亲密度越高，提升越慢（像真实关系）
- 伤害后恢复需要更多正向互动
- 亲密度下降有缓冲，不会一次掉很多

---

### 任务 2.4：称呼动态生成

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-04 |
| **任务名称** | 实现称呼随亲密度动态变化 |
| **任务描述** | 在 LLM 的 system prompt 中注入当前亲密度等级和推荐称呼，让 AI 自然使用 |
| **交付物** | 集成到人格 prompt 中的动态称呼模板 |
| **验收标准** | 不同亲密度下，AI 对用户的称呼自然变化，不生硬 |
| **依赖** | T2-03 |
| **难度** | ⭐⭐ 简单偏中 |

**实现方式：**
- 不硬编码称呼，而是在 system prompt 中给出当前关系阶段和称呼建议
- 让 LLM 根据上下文自然选择
- 避免每次都叫同一个称呼，要自然多变

---

### 任务 2.5：长期记忆系统

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-05 |
| **任务名称** | 实现长期记忆存储与检索 |
| **任务描述** | 从对话中提取用户信息，存入 SQLite + ChromaDB，支持语义检索召回 |
| **交付物** | `core/memory/long_term.py` |
| **验收标准** | 用户提到过的事情，在后续对话中能被 AI 自然回忆并提及 |
| **依赖** | T1-07 |
| **难度** | ⭐⭐⭐⭐ 较难 |

**工作流程：**
```
对话结束 → LLM 提取记忆点 → 存储到 SQLite + 向量化存入 ChromaDB
用户当前输入 → 向量检索相关记忆 → 注入 LLM prompt → AI 自然引用
```

**记忆提取 Prompt 模板：**
```
请从以下对话中提取值得记住的用户信息：
- 用户的基本信息（名字、年龄、职业等）
- 兴趣爱好
- 重要日程
- 人际关系
- 过往经历
- 情绪状态变化
- 用户的偏好和习惯

输出 JSON 格式：
{"memories": [{"type": "...", "content": "...", "importance": 1-5}]}
```

**存储结构：**
```sql
CREATE TABLE memories (
    id INTEGER PRIMARY KEY,
    type TEXT,           -- 基本信息/兴趣/日程/人际/经历/情绪/偏好
    content TEXT,        -- 记忆内容
    importance INTEGER,  -- 重要程度 1-5
    created_at DATETIME,
    last_recalled_at DATETIME,  -- 上次召回时间
    recall_count INTEGER DEFAULT 0
);
```

---

### 任务 2.6：情感语音合成

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-06 |
| **任务名称** | 接入带情感的 TTS |
| **任务描述** | 将 AI 当前情绪状态映射为语音参数，让语音语调随情绪变化 |
| **交付物** | `speech/emotion_tts.py` |
| **验收标准** | 开心时语调轻快，难过时语调低沉，生气时语速变化明显可感知 |
| **依赖** | T2-02, T1-05 |
| **难度** | ⭐⭐⭐⭐⭐ 难 |

**技术路线：**
1. **前期**：用云端情感 TTS（如火山引擎，支持情绪参数）
2. **后期**：切换到 **GPT-SoVITS** 或 **CosyVoice 2**
   - GPT-SoVITS：支持情感控制 + 音色克隆
   - CosyVoice 2：阿里开源，支持情感标签

**情绪 → 语音参数映射（示例）：**
| AI 情绪 | 语速 | 音调 | 音量 | 其他 |
|---------|------|------|------|------|
| 开心 | 稍快 | 偏高 | 正常 | 语调上扬 |
| 难过 | 慢 | 偏低 | 偏小 | 停顿多 |
| 生气 | 快 | 偏高 | 偏大 | 短句 |
| 平静 | 正常 | 正常 | 正常 | 平稳 |
| 期待 | 稍快 | 稍高 | 正常 | 尾音上扬 |
| 疲惫 | 慢 | 偏低 | 偏小 | 气声 |

---

### 任务 2.7：主动话题系统

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-07 |
| **任务名称** | 实现多触发条件的主动话题 |
| **任务描述** | 建立触发器系统，在满足条件时让 AI 主动发起对话 |
| **交付物** | `core/proactive.py` |
| **验收标准** | 至少实现 3 种触发条件（沉默、时间、情绪），触发时 AI 主动说话自然不突兀 |
| **依赖** | T2-02, T2-05 |
| **难度** | ⭐⭐⭐ 中等 |

**触发器框架：**
```python
class Trigger(ABC):
    @abstractmethod
    def should_trigger(self, context) -> bool:
        pass
    @abstractmethod
    def generate_topic(self, context) -> str:
        pass

class SilenceTrigger(Trigger):
    # 用户超过 N 小时没说话
    def should_trigger(self, context):
        return context.silence_duration > 6 * 3600
    def generate_topic(self, context):
        # 根据时间、上次对话内容、AI 自身情绪生成话题
        ...
```

**触发条件清单：**
- 长时间沉默（如 ≥6 小时）
- 特定时间节点（早上问候、晚间关心、深夜提醒休息）
- 检测到用户情绪低落
- 重要日程前提醒
- AI 自身想分享（随机概率，低频率）
- 天气剧烈变化提醒

---

### 任务 2.8：语音唤醒与打断

| 项 | 内容 |
|----|------|
| **任务 ID** | T2-08 |
| **任务名称** | 实现名字唤醒 + 语音打断 |
| **任务描述** | 使用唤醒词检测引擎，实现 AI 说话时用户可打断 |
| **交付物** | `speech/wake_word.py` + `speech/interruption.py` |
| **验收标准** | 用户说名字能唤醒；AI 说话时用户开口，AI 停止播放 |
| **依赖** | T1-06 |
| **难度** | ⭐⭐⭐⭐⭐ 难 |

**技术方案：**
- **唤醒词检测**：用开源的 **Porcupine** 或 **openWakeWord**，支持自定义唤醒词
- **打断检测**：AI 播放语音时持续监听麦克风，检测到用户语音能量超过阈值时停止播放

**普通麦克风的局限：**
- 回声消除（AEC）能力弱，可能误触发
- 建议后期升级麦克风或增加声学回声消除软件处理

---

### 第二阶段完成标志

✅ 能识别用户情绪，AI 有独立情绪，语音语调随情绪变化  
✅ 亲密度五级可运行，称呼自然变化  
✅ 能记住并在合适时机主动提及用户信息  
✅ 能主动发起话题，能被名字唤醒，能被打断

---

## 第三阶段：形象与体验升级

**阶段目标**：Live2D 形象生动，整体体验精致，接近最终形态

### 任务 3.1：Live2D 基础集成

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-01 |
| **任务名称** | 集成 Live2D 虚拟形象 |
| **任务描述** | 在 Electron 前端中嵌入 Live2D Cubism SDK，加载一个基础模型 |
| **交付物** | 窗口中显示可动 Live2D 形象 |
| **验收标准** | 形象正常渲染，可播放待机动画 |
| **依赖** | T1-08 |
| **难度** | ⭐⭐⭐ 中等 |

**技术栈：**
- Live2D Cubism Web SDK
- 需要准备或购买/下载 Live2D 模型文件（.moc3 格式）
- 可用免费模型先占位，后期替换

---

### 任务 3.2：表情情绪联动

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-02 |
| **任务名称** | Live2D 表情随 AI 情绪变化 |
| **任务描述** | Python 情绪系统 → WebSocket → 前端 Live2D 表情切换 |
| **交付物** | 情绪变化时，Live2D 表情同步切换 |
| **验收标准** | AI 开心时形象微笑，难过时表情低落，至少 4 种表情可切换 |
| **依赖** | T3-01, T2-02 |
| **难度** | ⭐⭐⭐ 中等 |

**映射关系：**
| AI 情绪 | Live2D 表情 | 辅助效果 |
|---------|-----------|---------|
| 开心 | 微笑 + 眼睛弯 | 亮度稍高 |
| 难过 | 眉毛下垂 + 嘴角向下 | 色调偏冷 |
| 生气 | 眉毛紧皱 + 嘴微抿 | 轻微抖动 |
| 平静 | 自然表情 | 正常 |
| 期待 | 眼睛微微睁大 | 轻微前倾 |

---

### 任务 3.3：基础动作

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-03 |
| **任务名称** | 加入眨眼、点头等基础动作 |
| **任务描述** | 实现随机眨眼、说话时轻微点头、思考时歪头 |
| **交付物** | 形象有自然的小动作，不僵硬 |
| **验收标准** | 待机时自然眨眼，说话时有头部微动 |
| **依赖** | T3-01 |
| **难度** | ⭐⭐ 简单偏中 |

---

### 任务 3.4：系统托盘常驻

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-04 |
| **任务名称** | 开机自启 + 托盘隐藏 |
| **任务描述** | 应用开机自动启动，平时隐藏到系统托盘，唤醒时弹出 |
| **交付物** | 托盘图标 + 开机自启功能 |
| **验收标准** | 重启电脑后应用自动运行并隐藏在托盘；唤醒后窗口弹出 |
| **依赖** | T2-08 |
| **难度** | ⭐⭐ 简单偏中 |

**技术要点：**
- 用 Electron 的 Tray API
- 开机自启用 Windows 注册表或启动文件夹
- 唤醒 → 窗口从托盘弹出 → 形象点亮 → 轻应一声

---

### 任务 3.5：唤醒点亮交互

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-05 |
| **任务名称** | 唤醒时形象点亮 + 轻应一声 |
| **任务描述** | 检测到唤醒词 → 窗口弹出 → Live2D 从休眠态切换到活跃态 → 播放轻应声 |
| **交付物** | 完整的唤醒响应流程 |
| **验收标准** | 唤醒后 1 秒内形象点亮并发出轻应声 |
| **依赖** | T3-04, T2-08 |
| **难度** | ⭐⭐⭐ 中等 |

---

### 任务 3.6：桌面通知

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-06 |
| **任务名称** | 接入 Windows 桌面通知 |
| **任务描述** | 定时提醒、天气提醒等通过 Windows 通知推送 |
| **交付物** | 通知功能可用 |
| **验收标准** | 设置提醒后，到时间弹出 Windows 通知 |
| **依赖** | T3-04 |
| **难度** | ⭐ 简单 |

---

### 任务 3.7：酷狗音乐控制

| 项 | 内容 |
|----|------|
| **任务 ID** | T3-07 |
| **任务名称** | 实现酷狗音乐基础控制 |
| **任务描述** | 通过语音指令实现点歌、播放、暂停、切歌、调音量 |
| **交付物** | `tools/music.py` |
| **验收标准** | 用户说“播放 XXX”后，酷狗音乐开始播放 |
| **依赖** | T1-06 |
| **难度** | ⭐⭐⭐ 中等 |

**实现思路：**
1. 研究酷狗音乐是否有官方开放 API
2. 若无，用 Windows 模拟按键 + 剪贴板搜索实现
3. 后期可研究酷狗音乐的 URI Scheme 或 MCP 协议

---

### 第三阶段完成标志

✅ Live2D 形象有表情、有动作、有情绪联动  
✅ 开机自启、托盘隐藏、唤醒点亮完整闭环  
✅ 能通过语音控制音乐  
✅ 桌面通知可用

---

## 第四阶段：工具与扩展

**阶段目标**：扩展实用工具和网络能力

### 任务 4.1：天气查询

| 项 | 内容 |
|----|------|
| **任务 ID** | T4-01 |
| **任务名称** | 接入天气 API |
| **任务描述** | 接入和风天气或 OpenWeatherMap，支持当前天气和预报查询 |
| **交付物** | `tools/weather.py` |
| **验收标准** | 用户问天气，AI 能准确回答当前和未来天气 |
| **依赖** | T1-06 |
| **难度** | ⭐ 简单 |

---

### 任务 4.2：定时提醒

| 项 | 内容 |
|----|------|
| **任务 ID** | T4-02 |
| **任务名称** | 实现定时提醒功能 |
| **任务描述** | 用户说“提醒我 X 时间做 Y”，到时间通过语音和通知提醒 |
| **交付物** | `tools/reminder.py` |
| **验收标准** | 提醒到时间准确触发，支持取消和修改 |
| **依赖** | T3-06 |
| **难度** | ⭐⭐ 简单偏中 |

---

### 任务 4.3：情绪推荐音乐

| 项 | 内容 |
|----|------|
| **任务 ID** | T4-03 |
| **任务名称** | 根据用户情绪推荐音乐 |
| **任务描述** | 结合当前用户情绪和场景，从酷狗音乐推荐合适歌曲 |
| **交付物** | 集成到音乐控制中的推荐逻辑 |
| **验收标准** | 用户难过时推荐舒缓治愈类歌曲，开心时推荐轻快类 |
| **依赖** | T3-07, T2-01 |
| **难度** | ⭐⭐⭐ 中等 |

**情绪 → 音乐推荐映射（示例）：**
| 情绪 | 推荐风格 |
|------|---------|
| 开心 | 轻快流行、电子 |
| 难过 | 治愈系、抒情慢歌 |
| 生气 | 平静纯音乐、钢琴 |
| 焦虑 | 白噪音、轻音乐 |
| 疲惫 | 民谣、舒缓爵士 |
| 期待 | 励志、节奏感强 |

---

### 任务 4.4：网络资讯查询

| 项 | 内容 |
|----|------|
| **任务 ID** | T4-04 |
| **任务名称** | 实现简单网络资讯查询 |
| **任务描述** | 聊天中用户问到实时信息（新闻、百科等），AI 能查并回应 |
| **交付物** | `tools/web_info.py` |
| **验收标准** | 用户问“今天有什么新闻”，AI 能给出简要资讯 |
| **依赖** | T1-06 |
| **难度** | ⭐⭐ 简单偏中 |

**实现方式：**
- 用 LLM 的 Function Calling 能力
- 或接入搜索引擎 API（如 Bing Search API）
- 或简单爬取新闻 RSS 源

---

### 任务 4.5：设备控制接口预留

| 项 | 内容 |
|----|------|
| **任务 ID** | T4-05 |
| **任务名称** | 预留 MCP 协议扩展接口 |
| **任务描述** | 设计工具调用统一接口，为后期控制电脑/手机/智能家居留扩展点 |
| **交付物** | `tools/base.py` 工具抽象层 + 注册机制 |
| **验收标准** | 新工具可通过注册方式接入，无需改动核心代码 |
| **依赖** | T1-01 |
| **难度** | ⭐⭐⭐ 中等 |

---

### 第四阶段完成标志

✅ 天气、提醒、音乐、资讯查询全部可用  
✅ 工具扩展框架就绪，后续可快速接入新能力

---

## 第五阶段：本地化与产品化

**阶段目标**：完全本地运行，具备产品化雏形

### 任务 5.1：本地 LLM 部署

| 项 | 内容 |
|----|------|
| **任务 ID** | T5-01 |
| **任务名称** | 本地部署大语言模型 |
| **任务描述** | 用 Ollama 或 vLLM 部署 Qwen 系列模型，替换云端 API |
| **交付物** | 本地 LLM 适配器 |
| **验收标准** | 无网络时对话正常，响应延迟可接受，中文质量不低于云端 80% |
| **依赖** | 第四阶段完成 |
| **难度** | ⭐⭐⭐⭐ 较难 |

**注意：**
- 需要显卡至少 8GB 显存（7B 量化）
- 若配置不足，先用 1.5B~3B 小模型，体验会下降
- 建议后期购置带 12GB+ 显存的设备或独立服务器

---

### 任务 5.2：本地 ASR 部署

| 项 | 内容 |
|----|------|
| **任务 ID** | T5-02 |
| **任务名称** | 切换到本地 FunASR |
| **任务描述** | 部署 FunASR 模型，替换云端语音识别 |
| **交付物** | 本地 ASR 适配器 |
| **验收标准** | 离线可语音识别，准确率和实时性满足日常对话 |
| **依赖** | 第四阶段完成 |
| **难度** | ⭐⭐⭐ 中等 |

---

### 任务 5.3：本地情感 TTS 部署

| 项 | 内容 |
|----|------|
| **任务 ID** | T5-03 |
| **任务名称** | 切换到本地情感 TTS |
| **任务描述** | 部署 GPT-SoVITS 或 CosyVoice 2，克隆参考音色，替换云端 TTS |
| **交付物** | 本地情感 TTS 适配器 |
| **验收标准** | 离线可合成带情感的语音，音色与设定一致 |
| **依赖** | 第四阶段完成 |
| **难度** | ⭐⭐⭐⭐⭐ 难 |

**工作流程：**
1. 准备参考音频（温柔女声）
2. 训练/微调音色模型
3. 集成到项目，实现情绪 → 语音参数控制

---

### 任务 5.4：数据迁移工具

| 项 | 内容 |
|----|------|
| **任务 ID** | T5-04 |
| **任务名称** | 实现一键数据迁移 |
| **任务描述** | 导出/导入所有本地数据（记忆、情绪档案、亲密度、配置） |
| **交付物** | 迁移脚本或界面功能 |
| **验收标准** | 迁移到新电脑后，AI 记忆和关系状态完整恢复 |
| **依赖** | 第二阶段完成 |
| **难度** | ⭐⭐ 简单偏中 |

---

### 任务 5.5：配置管理界面

| 项 | 内容 |
|----|------|
| **任务 ID** | T5-05 |
| **任务名称** | 提供配置管理界面 |
| **任务描述** | 允许用户调整人格参数、情绪敏感度、亲密度规则、触发条件等 |
| **交付物** | 设置页面 |
| **验收标准** | 非技术人员也能通过界面修改核心参数 |
| **依赖** | 第三阶段完成 |
| **难度** | ⭐⭐⭐ 中等 |

---

### 第五阶段完成标志

✅ 断网也能正常对话（本地 LLM + 本地 ASR + 本地 TTS）  
✅ 数据可迁移  
✅ 配置可调整  
✅ 具备产品化雏形

---

## 任务优先级与依赖总览

```
T1-01 项目搭建
  ├── T1-02 LLM 适配器
  │     └── T1-03 人格 Prompt
  ├── T1-04 ASR
  ├── T1-05 TTS
  ├── T1-06 对话管理（依赖 1-03, 1-04, 1-05）
  │     ├── T1-07 对话历史
  │     └── T1-08 桌面窗口
  │
  ├── T2-01 用户情绪识别（依赖 1-06）
  │     └── T2-02 AI 情绪系统
  │           ├── T2-03 亲密度系统
  │           │     └── T2-04 称呼动态生成
  │           ├── T2-06 情感 TTS
  │           └── T2-07 主动话题
  ├── T2-05 长期记忆（依赖 1-07）
  └── T2-08 唤醒+打断（依赖 1-06）
        └── T3-04 托盘常驻
              ├── T3-05 唤醒点亮
              └── T3-06 桌面通知

T3-01 Live2D 集成（依赖 1-08）
  ├── T3-02 表情情绪联动（依赖 3-01, 2-02）
  └── T3-03 基础动作（依赖 3-01）

T3-07 音乐控制（依赖 1-06）
  └── T4-03 情绪推荐音乐（依赖 3-07, 2-01）

T4-01 天气（依赖 1-06）
T4-02 提醒（依赖 3-06）
T4-04 资讯查询（依赖 1-06）
T4-05 工具框架（依赖 1-01）

T5-01 本地 LLM（依赖 第四阶段完成）
T5-02 本地 ASR（依赖 第四阶段完成）
T5-03 本地 TTS（依赖 第四阶段完成）
T5-04 数据迁移（依赖 第二阶段完成）
T5-05 配置界面（依赖 第三阶段完成）
```

---

## 任务清单汇总表

| 阶段 | 任务数 | 预估总时间 | 难度范围 |
|------|--------|-----------|---------|
| 第一阶段 | 8 | 4~6 周 | ⭐~⭐⭐⭐ |
| 第二阶段 | 8 | 8~12 周 | ⭐⭐~⭐⭐⭐⭐⭐ |
| 第三阶段 | 7 | 6~10 周 | ⭐~⭐⭐⭐ |
| 第四阶段 | 5 | 3~4 周 | ⭐~⭐⭐⭐ |
| 第五阶段 | 5 | 4~8 周 | ⭐⭐~⭐⭐⭐⭐⭐ |

---

这份任务清单可以直接用于：
1. **与 AI 协作**：把每个任务 ID 的内容发给 AI，让它帮你写代码
2. **进度跟踪**：完成一项勾一项
3. **难度预估**：提前知道哪些模块需要更多精力

建议从 **T1-01** 开始，一项一项来。需要我帮你先启动 T1-01 吗？
